<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/light_cian_bg">

    <!-- máscara para que respete los radios de esquina -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />  <!-- el color da igual -->
            <corners android:radius="100dp"/>               <!-- mismo radio que tu barra -->
        </shape>
    </item>

</ripple>