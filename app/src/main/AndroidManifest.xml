<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArcaDeBits"
        tools:targetApi="31">

        <!-- Facebook App ID -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" /> <!-- Facebook Client Token -->
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token" /> <!-- Facebook Login activity (requerido) -->
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:exported="false"
            android:label="@string/app_name" /> <!-- o true si lo deseas -->
        <!-- Facebook Custom Tab (para Login con navegador) -->
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="fb1141084374730130" />
            </intent-filter>
        </activity> <!-- >>> FileProvider para compartir fotos/vídeos (requerido por el SDK) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".ui.view.SupportActivity"
            android:exported="false" />
        <activity
            android:name=".ui.view.ProductDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.view.SocialMediasActivity"
            android:exported="false" />
        <activity
            android:name=".ui.view.MainActivity"
            android:exported="false" />
        <activity
            android:name=".ui.view.RecoverAccountActivity"
            android:exported="false" />
        <activity
            android:name=".ui.view.SignUpActivity"
            android:exported="false"
            android:theme="@style/Theme.ArcaDeBits" />
        <activity
            android:name=".ui.view.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".ui.view.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>